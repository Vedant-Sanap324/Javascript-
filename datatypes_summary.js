// 7 types of datatypes in javascript
//there is no special datatype for integers, floats, or doubles. All numbers are represented as floating point values.
//special type of datatype called symbols
//eg:
const id = Symbol('123')
const anotherid = Symbol('123')
console.log(id === anotherid) // false

const heros = ['thor', 'spiderman', 'ironman'];
let object = {
    name: 'vedant',
    age: 20,
}
const myfuction = function()
{ console.log('hello world')}

myfuction() // hello world